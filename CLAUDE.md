# プロジェクト概要

このリポジトリはShopify公式テーマ「Dawn」のfork版です。
70年の歴史を持つ老舗肉屋のECサイト構築を目的としています。

将来的にDawn本家のアップデートをmergeで取り込むため、
**衝突を最小化する設計**を最優先します。

---

## ブランチ運用

- `main`：Dawn公式追従用。直接編集しない。upstreamからのmerge専用。
- `develop`：作業用ブランチ。すべてのカスタマイズはここから派生させる。
- 作業ブランチは `develop` から切り、PR経由で `develop` にマージする。

---

## 編集ルール（最重要）

### 原則1：既存ファイルの直接編集は最終手段
新規ファイル追加で実現できないか必ず先に検討してください。
「既存ファイルを編集する案」より「新規ファイルを追加する案」を常に優先してください。

### 原則2：CSS は集約する
- すべてのカスタムスタイルは `assets/custom.css` に記述する
- `layout/theme.liquid` への組み込みは読み込み1行のみ
- Dawnの既存CSSファイル（`base.css` など）は編集しない
- セクション固有のスタイルも `custom.css` 内にコメントで区切って記述する

### 原則3：JS は集約する
- すべてのカスタムJSは `assets/custom.js` に記述する
- Dawnの既存JSファイルは編集しない

### 原則4：新規セクション・スニペットには命名規則を適用
- セクション：`sections/custom-*.liquid`
- スニペット：`snippets/custom-*.liquid`
- 既存ファイル名と被らないようにする

### 原則5：やむを得ず既存ファイルを編集する場合
必ず以下のコメントマーカーで変更箇所を囲んでください。

Liquidの場合：
{% comment %} CUSTOM START: 何をなぜ変更したかの簡潔な説明 {% endcomment %}
（カスタムコード）
{% comment %} CUSTOM END {% endcomment %}

CSS/JSの場合：
/* CUSTOM START: 説明 */
（カスタムコード）
/* CUSTOM END */

### 原則6：編集前に現状確認
Dawnのバージョンによってファイル構造が異なる可能性があります。
編集対象ファイルの現状を必ず最初に確認してから作業してください。

---

## ブランドガイドライン

### コンセプト
70年の歴史を持つ老舗肉屋。伝統と職人性を重んじる和の佇まい。
モダンすぎず、しかし古臭くもない、品格のあるデザイン。

### カラーパレット
`assets/custom.css` の `:root` に CSS変数として定義する：

- メインカラー：深い赤茶色 `#6B2D1F`
- アクセントカラー：黒に近い濃茶 `#2B1810`
- ベースカラー：オフホワイト `#F5F1EA`
- 補助カラー：くすんだ金色 `#A87F3A`

新規スタイルでは必ずこれらのCSS変数を参照し、ハードコードしないこと。

### タイポグラフィ
- 和文：游明朝（見出し）／游ゴシック（本文）系統
- 行間・字間は和文に最適化された値を使用
- 縦書きセクションには `writing-mode: vertical-rl` を活用

---

## 実装時の確認事項

新しい機能を実装する前に、以下を自問してください：

1. これは新規ファイル追加で実現できないか？
2. 既存のDawn機能と重複していないか？
3. テーマカスタマイザー（管理画面）で設定変更できる作りになっているか？
4. ハードコードされた色やフォントはないか？
5. CUSTOMコメントマーカーで変更箇所が明示されているか？

---

## やってはいけないこと

- `config/settings_schema.json` の既存項目の削除・改変（追加はOK）
- Dawnの既存セクション・スニペット・テンプレートファイルの削除
- `package.json`、`.github/` 配下、`shopify.theme.toml` の改変
- 大規模なリファクタリング（Dawnの構造を尊重する）

---

## 参考情報

- Dawn公式リポジトリ：https://github.com/Shopify/dawn
- Shopify Liquid公式ドキュメント：https://shopify.dev/docs/api/liquid
